{
  "name": "dsh-terminal-xterm-vendor",
  "version": "0.0.0",
  "private": true,
  "description": "Build inputs for the vendored xterm.js single-file classic bundle shipped inside dsh-terminal (lib/vendor/xterm.js + lib/vendor/xterm.css). Rebuild: npm install, then esbuild the entry and copy the stylesheet (see README in packages/dsh-terminal). Never published.",
  "type": "module",
  "dependencies": {
    "@xterm/addon-fit": "0.10.0",
    "@xterm/xterm": "5.5.0"
  }
}
